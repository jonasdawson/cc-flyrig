# Statusline capture coverage — cc-{{ report.cc_version }}

Captured at: {{ report.captured_at or 'n/a' }}

Coverage for the statusline event family: observed {{ report.observed }}/{{ report.total_events }} events · {{ report.missing }} expected-but-missing · {{ report.not_attempted }} not-attempted.
{% if report.missing %}

> ⚠️ Some statusline event family events the battery expected were not captured — see **expected-but-missing** below.
{% endif %}

## Events

| Event | Status | Payloads |
| --- | --- | ---: |
{% for row in report.rows %}
| {{ row.event }} | {{ row.status }} | {{ row.count }} |
{% endfor %}
